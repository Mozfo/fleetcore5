# .env.test.example - Template for test environment configuration
# Copy to .env.test and fill with actual values
#
# 🤖 CI/CD CONFIGURATION
# The following 2 variables MUST be configured as GitHub Secrets:
# - DATABASE_URL (critical - use test/staging DB, not production)
# - TEST_USER_PASSWORD (strong password for programmatic user creation)
#
# See docs/operations/CI_CD_SETUP.md for detailed setup instructions

# Database
# GitHub Secret: DATABASE_URL
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE?pgbouncer=true&connection_limit=1

# Application URLs (public - hardcoded in CI)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Test user credentials (will be created programmatically)
# GitHub Secret: TEST_USER_PASSWORD
TEST_USER_EMAIL_PREFIX=test-fleetcore
TEST_USER_PASSWORD=CHANGE_ME_STRONG_PASSWORD
TEST_USER_FIRST_NAME=Test
TEST_USER_LAST_NAME=FleetCore

# Test organization (public - hardcoded in CI)
TEST_ORG_NAME=FleetCore Test Organization

# Cache configuration (1 hour in milliseconds) (public - hardcoded in CI)
TEST_CREDENTIALS_CACHE_DURATION=3600000
